<header class="header">
        <h1>File-Sharing-App</h1>
        <nav>
                {{#each navbar}}
                        <a id={{this.id}} href="{{this.link}}">{{this.name}}</a>
                {{/each}}
        </nav>
</header>